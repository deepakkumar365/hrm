╔══════════════════════════════════════════════════════════════════════════════╗
║         HRMS DATABASE MIGRATION - QUICK START GUIDE                           ║
║         Leave Allocation & Employee Groups Setup                             ║
╚══════════════════════════════════════════════════════════════════════════════╝

🔥 CRITICAL: Database Not Initialized - Migration Required

Before you can run the application, you must run the database migration to create
the new tables for Employee Groups and Leave Allocation features.

═══════════════════════════════════════════════════════════════════════════════

📋 MIGRATION STATUS: ✅ READY TO RUN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

All migration chain issues have been fixed. The chain is now linear and complete.

═══════════════════════════════════════════════════════════════════════════════

🚀 HOW TO RUN THE MIGRATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

OPTION 1: Double-click the batch file (Easiest)
   Location: D:\Projects\HRMS\hrm\RUN_MIGRATION.bat
   
   → Simply double-click the file and wait for completion

OPTION 2: Use PyCharm Terminal
   1. Open PyCharm
   2. Open Terminal (View → Tool Windows → Terminal)
   3. Run this command:
      
      python -m flask db upgrade
   
   4. Wait for "Upgrade done" message

OPTION 3: Command Prompt / PowerShell
   1. Press Windows + R
   2. Type: cmd  (or powershell)
   3. Paste this:
      
      cd /d "D:\Projects\HRMS\hrm" && python -m flask db upgrade
   
   4. Press Enter and wait

OPTION 4: Use Helper Script
   python verify_and_migrate.py

═══════════════════════════════════════════════════════════════════════════════

✅ WHAT GETS CREATED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The migration will create these database tables:

   ✓ hrm_employee_group
     - For grouping employees (e.g., "Shift A", "Department X")
     - Companies can create multiple groups
     - Each group can have different leave allocations

   ✓ hrm_designation_leave_allocation
     - Leave days assigned by job designation
     - Example: "Manager" gets 20 days, "Junior" gets 15 days
     
   ✓ hrm_employee_group_leave_allocation
     - Leave days assigned by employee group
     - Example: "Shift A" gets 18 days, "Shift B" gets 16 days

   ✓ hrm_employee_leave_allocation
     - Individual employee overrides
     - For custom leave allocations per employee
     - Example: Senior manager gets 25 days (override)

   ✓ employee_group_id column added to hrm_employee table
     - Links each employee to their assigned group

═══════════════════════════════════════════════════════════════════════════════

⏱️ EXPECTED TIME & WARNINGS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Expected Duration: 5-15 seconds

⚠️  IMPORTANT NOTES:
   - Do NOT close the terminal/window during migration
   - Do NOT interrupt the process
   - Your database will be locked during migration
   - If you have data, it will NOT be lost
   - Migrations are one-way (cannot be reversed without downgrade)

═══════════════════════════════════════════════════════════════════════════════

✨ AFTER SUCCESSFUL MIGRATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. START THE APPLICATION
   
   python main.py
   
   or from PyCharm: Run → Run 'main'

2. LOGIN TO THE APPLICATION
   
   Use your admin credentials to login

3. VERIFY NEW FEATURES ARE ACCESSIBLE
   
   Navigate to: Masters → Employee Groups
   You should see the new menu option
   
   Navigate to: Masters → Leave Allocation Configuration
   You should see this new configuration area

4. CHECK ACCESS CONTROL
   
   These roles can access Employee Groups & Leave Allocation:
     ✓ Super Admin
     ✓ HR Manager
     ✓ Tenant Admin
   
   Other roles will see 403 Forbidden (expected)

5. TEST CREATING AN EMPLOYEE GROUP
   
   1. Go to Masters → Employee Groups
   2. Click "Add New" or "Create Group"
   3. Enter group name (e.g., "Day Shift")
   4. Save and verify it appears in the list

═══════════════════════════════════════════════════════════════════════════════

🔍 TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ISSUE: "Database not initialized. Please run: flask db upgrade"
SOLUTION:
   → You're seeing this message because migration hasn't run yet
   → Follow the "HOW TO RUN THE MIGRATION" section above

ISSUE: "ModuleNotFoundError: No module named 'flask'"
SOLUTION:
   → Dependencies not installed
   → Run: pip install -r requirements.txt
   → Then retry migration

ISSUE: "connection refused" or "could not connect to server"
SOLUTION:
   → PostgreSQL not running
   → Check your database connection string in .env
   → Verify PostgreSQL is running (Windows: Services)

ISSUE: "permission denied" or "access denied"
SOLUTION:
   → Check DATABASE_URL environment variable
   → Verify PostgreSQL user has proper permissions
   → Try with a different user account if available

ISSUE: "psycopg2.ProgrammingError: relation 'hrm_...' does not exist"
SOLUTION:
   → Database schema is incomplete
   → Try running: flask db upgrade --sql
   → Run: flask db downgrade (then upgrade again)
   → Last resort: Reset database and retry

═══════════════════════════════════════════════════════════════════════════════

📞 SUPPORT & DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For detailed technical information, see:
   → MIGRATION_FIXES_APPLIED.md - Technical breakdown of fixes
   → MIGRATION_CHAIN_SUMMARY.txt - Full migration chain
   → .zencoder/rules/repo.md - Repository structure

═══════════════════════════════════════════════════════════════════════════════

✅ YOU'RE READY!

Pick any method above and run the migration now.
The application will work perfectly once the migration completes!

═══════════════════════════════════════════════════════════════════════════════